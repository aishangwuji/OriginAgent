{
  "mcpServers": {
    "gitnexus": {
      "command": "npx",
      "args": ["-y", "gitnexus@latest", "mcp"],
      "description": "GitNexus code intelligence — query, context, impact analysis, rename, detect_changes"
    }
  }
}
